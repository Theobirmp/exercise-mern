import {styled} from '@mui/system'
import { Box } from '@mui/material'
export const PageContainer=styled(Box)({
    // height:'100vh',
    width:'100%',
    display:'grid',
    placeContent:'center'
})